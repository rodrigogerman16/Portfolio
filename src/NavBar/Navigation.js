import NavLinks from "./NavLinks";
import classes from "./NavBar.module.css"

const Navigation = () => {
    return ( 
        <nav>
            <NavLinks/>
        </nav>
     );
}

export default Navigation;